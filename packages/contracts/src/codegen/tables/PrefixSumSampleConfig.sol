// SPDX-License-Identifier: MIT
pragma solidity >=0.8.0;

/* Autogenerated file. Do not edit manually. */

// Import schema type
import { SchemaType } from "@latticexyz/schema-type/src/solidity/SchemaType.sol";

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { Schema, SchemaLib } from "@latticexyz/store/src/Schema.sol";
import { PackedCounter, PackedCounterLib } from "@latticexyz/store/src/PackedCounter.sol";

bytes32 constant _tableId = bytes32(abi.encodePacked(bytes16(""), bytes16("PrefixSumSampleC")));
bytes32 constant PrefixSumSampleConfigTableId = _tableId;

library PrefixSumSampleConfig {
  /** Get the table's schema */
  function getSchema() internal pure returns (Schema) {
    SchemaType[] memory _schema = new SchemaType[](1);
    _schema[0] = SchemaType.UINT32_ARRAY;

    return SchemaLib.encode(_schema);
  }

  function getKeySchema() internal pure returns (Schema) {
    SchemaType[] memory _schema = new SchemaType[](1);
    _schema[0] = SchemaType.BYTES32;

    return SchemaLib.encode(_schema);
  }

  /** Get the table's metadata */
  function getMetadata() internal pure returns (string memory, string[] memory) {
    string[] memory _fieldNames = new string[](1);
    _fieldNames[0] = "accWeights";
    return ("PrefixSumSampleConfig", _fieldNames);
  }

  /** Register the table's schema */
  function registerSchema() internal {
    StoreSwitch.registerSchema(_tableId, getSchema(), getKeySchema());
  }

  /** Register the table's schema (using the specified store) */
  function registerSchema(IStore _store) internal {
    _store.registerSchema(_tableId, getSchema(), getKeySchema());
  }

  /** Set the table's metadata */
  function setMetadata() internal {
    (string memory _tableName, string[] memory _fieldNames) = getMetadata();
    StoreSwitch.setMetadata(_tableId, _tableName, _fieldNames);
  }

  /** Set the table's metadata (using the specified store) */
  function setMetadata(IStore _store) internal {
    (string memory _tableName, string[] memory _fieldNames) = getMetadata();
    _store.setMetadata(_tableId, _tableName, _fieldNames);
  }

  /** Get accWeights */
  function get(bytes32 sampleId) internal view returns (uint32[] memory accWeights) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    bytes memory _blob = StoreSwitch.getField(_tableId, _keyTuple, 0);
    return (SliceLib.getSubslice(_blob, 0, _blob.length).decodeArray_uint32());
  }

  /** Get accWeights (using the specified store) */
  function get(IStore _store, bytes32 sampleId) internal view returns (uint32[] memory accWeights) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    bytes memory _blob = _store.getField(_tableId, _keyTuple, 0);
    return (SliceLib.getSubslice(_blob, 0, _blob.length).decodeArray_uint32());
  }

  /** Set accWeights */
  function set(bytes32 sampleId, uint32[] memory accWeights) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    StoreSwitch.setField(_tableId, _keyTuple, 0, EncodeArray.encode((accWeights)));
  }

  /** Set accWeights (using the specified store) */
  function set(IStore _store, bytes32 sampleId, uint32[] memory accWeights) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    _store.setField(_tableId, _keyTuple, 0, EncodeArray.encode((accWeights)));
  }

  /** Get the length of accWeights */
  function length(bytes32 sampleId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    uint256 _byteLength = StoreSwitch.getFieldLength(_tableId, _keyTuple, 0, getSchema());
    return _byteLength / 4;
  }

  /** Get the length of accWeights (using the specified store) */
  function length(IStore _store, bytes32 sampleId) internal view returns (uint256) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    uint256 _byteLength = _store.getFieldLength(_tableId, _keyTuple, 0, getSchema());
    return _byteLength / 4;
  }

  /** Get an item of accWeights (unchecked, returns invalid data if index overflows) */
  function getItem(bytes32 sampleId, uint256 _index) internal view returns (uint32) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    bytes memory _blob = StoreSwitch.getFieldSlice(_tableId, _keyTuple, 0, getSchema(), _index * 4, (_index + 1) * 4);
    return (uint32(Bytes.slice4(_blob, 0)));
  }

  /** Get an item of accWeights (using the specified store) (unchecked, returns invalid data if index overflows) */
  function getItem(IStore _store, bytes32 sampleId, uint256 _index) internal view returns (uint32) {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    bytes memory _blob = _store.getFieldSlice(_tableId, _keyTuple, 0, getSchema(), _index * 4, (_index + 1) * 4);
    return (uint32(Bytes.slice4(_blob, 0)));
  }

  /** Push an element to accWeights */
  function push(bytes32 sampleId, uint32 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    StoreSwitch.pushToField(_tableId, _keyTuple, 0, abi.encodePacked((_element)));
  }

  /** Push an element to accWeights (using the specified store) */
  function push(IStore _store, bytes32 sampleId, uint32 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    _store.pushToField(_tableId, _keyTuple, 0, abi.encodePacked((_element)));
  }

  /** Pop an element from accWeights */
  function pop(bytes32 sampleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    StoreSwitch.popFromField(_tableId, _keyTuple, 0, 4);
  }

  /** Pop an element from accWeights (using the specified store) */
  function pop(IStore _store, bytes32 sampleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    _store.popFromField(_tableId, _keyTuple, 0, 4);
  }

  /** Update an element of accWeights at `_index` */
  function update(bytes32 sampleId, uint256 _index, uint32 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    StoreSwitch.updateInField(_tableId, _keyTuple, 0, _index * 4, abi.encodePacked((_element)));
  }

  /** Update an element of accWeights (using the specified store) at `_index` */
  function update(IStore _store, bytes32 sampleId, uint256 _index, uint32 _element) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    _store.updateInField(_tableId, _keyTuple, 0, _index * 4, abi.encodePacked((_element)));
  }

  /** Tightly pack full data using this table's schema */
  function encode(uint32[] memory accWeights) internal pure returns (bytes memory) {
    uint40[] memory _counters = new uint40[](1);
    _counters[0] = uint40(accWeights.length * 4);
    PackedCounter _encodedLengths = PackedCounterLib.pack(_counters);

    return abi.encodePacked(_encodedLengths.unwrap(), EncodeArray.encode((accWeights)));
  }

  /** Encode keys as a bytes32 array using this table's schema */
  function encodeKeyTuple(bytes32 sampleId) internal pure returns (bytes32[] memory _keyTuple) {
    _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;
  }

  /* Delete all data for given keys */
  function deleteRecord(bytes32 sampleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /* Delete all data for given keys (using the specified store) */
  function deleteRecord(IStore _store, bytes32 sampleId) internal {
    bytes32[] memory _keyTuple = new bytes32[](1);
    _keyTuple[0] = sampleId;

    _store.deleteRecord(_tableId, _keyTuple);
  }
}
